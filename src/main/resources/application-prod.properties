spring.application.name=SurfSpace-App

# Configuración segura para producción
# SIN INTERFAZ pero CON documentación JSON en producción
springdoc.swagger-ui.path=/docs
springdoc.swagger-ui.enabled=false  # ? SIN interfaz visual
springdoc.api-docs.enabled=true     # ? PERO SÍ documentación para herramientas

# Seguridad adicional - solo mostrar endpoints públicos
springdoc.paths-to-exclude=/admin/**, /internal/**
